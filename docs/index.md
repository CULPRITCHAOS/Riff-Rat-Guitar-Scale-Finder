# Riff Rat

Welcome to **Riff Rat**, an interactive guitar fretboard application. This GitHub Pages site hosts the latest build of the app.

To run locally:
1. install dependencies with `npm install`
2. start the dev server with `npm run dev`

For a production build, run `npm run build` and serve the `dist` directory.

